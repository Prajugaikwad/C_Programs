#include <stdio.h>

int main() 
{
   // printf() displays the string inside quotation
   int no = 11;
   //printf("%d\n",no);
   //printf("%d\n",sizeof(no));
  int *p = &no;
   // %p for adddress display and p is for pointer
  // printf("%p\n",&no);
   return 0;
}